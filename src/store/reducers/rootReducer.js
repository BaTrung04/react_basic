const initState = {
    users: [
        { id: 1, name: "BaTrung" },
        { id: 2, name: "Van a" },
    ],
};

const rootReducer = (state = initState, active) => {
    return state;
};
export default rootReducer;
